{{#if success_msg}}
    <div class="alert alert-success">{{success_msg}}</div>  <!-- success_msg is global var  -->
{{/if}}
{{#if error_msg}}
    <div class="alert alert-danger">{{error_msg}}</div>     <!-- error_msg is global var  -->
{{/if}}